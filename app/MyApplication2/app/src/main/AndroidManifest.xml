<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cwjwj.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="消息实时发布系统"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".TabActivity" />
        <activity
            android:name=".receiverActivity"
            android:label="消息实时发布系统"
            android:theme="@style/AppTheme" />
        <activity android:name=".group_manage.GroupInfoActivity" />
        <activity android:name=".receiver_manage.SendMailActivity" />
        <activity
            android:name=".MenuActivity"
            android:label="消息实时发布系统"
            android:theme="@style/AppTheme" />
        <activity android:name=".group_manage.AddGroupActivity" />
        <activity android:name=".receiver_manage.AddReceiverActivity"></activity>
    </application>

</manifest>